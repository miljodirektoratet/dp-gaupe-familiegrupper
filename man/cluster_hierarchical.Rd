% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_hierarchical.R
\name{cluster_hierarchical}
\alias{cluster_hierarchical}
\title{Hierarchical Clustering for Lynx Family Group Assignment}
\usage{
cluster_hierarchical(grouping_index, hclust_poly = 1, cut_height = 1)
}
\arguments{
\item{grouping_index}{A numeric matrix where values represent the ratio of actual
distance to distance rule threshold. Values < 1 indicate observations that should
be grouped together; values > 1 indicate observations that should be separated.}

\item{hclust_poly}{A numeric exponent applied to the grouping index before clustering.
Higher values increase separation between groups. Default is 1 (no transformation).}

\item{cut_height}{The height at which to cut the dendrogram to form clusters.
Default is 1, which groups observations with \code{grouping_index^hclust_poly < 1}.}
}
\value{
An integer vector of cluster assignments, with the same length as the
number of rows/columns in \code{grouping_index}.
}
\description{
Performs hierarchical clustering on a grouping index matrix to assign observations
to family groups based on spatial and temporal distance relationships.
}
\details{
The function:
\enumerate{
\item Applies a power transformation to the grouping index: \code{grouping_index^hclust_poly}
\item Converts the transformed matrix to a distance object
\item Performs hierarchical clustering using complete linkage (default)
\item Cuts the dendrogram at the specified height to create cluster assignments
}

The grouping index is typically calculated as \code{distance_matrix / distance_rule_matrix},
where distance_rule_matrix contains the maximum allowed distances for observation pairs
based on prey class and temporal separation.
}
\examples{
\dontrun{
# Create sample grouping index (ratio of actual to allowed distance)
grouping_index <- matrix(c(
  0.5, 0.8, 1.5,
  0.8, 0.5, 1.2,
  1.5, 1.2, 0.5
), nrow = 3, byrow = TRUE)

# Perform clustering
clusters <- cluster_hierarchical(grouping_index, hclust_poly = 1)
}

}
